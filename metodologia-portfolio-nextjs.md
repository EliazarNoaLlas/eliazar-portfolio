# 🚀 Metodología Profesional: Portfolio Senior Systems & AI Architect
### De la Investigación al Levantamiento Local en Next.js 15

---

## 📋 ÍNDICE DE FASES

| Fase | Nombre | Duración Estimada |
|------|--------|-------------------|
| 1 | Investigación y Benchmark | 1–2 días |
| 2 | Definición de Contenido y Narrativa | 1 día |
| 3 | Diseño UX/UI en Figma | 2–3 días |
| 4 | Setup del Proyecto Next.js | 2–4 horas |
| 5 | Arquitectura de Carpetas y Componentes | 1 día |
| 6 | Implementación por Secciones | 3–5 días |
| 7 | Integración GitHub API + Datos | 1 día |
| 8 | Revisión, Pulido y Pruebas Locales | 1 día |

---

## 🔍 FASE 1 — Investigación y Benchmark

### 1.1 Objetivo
Antes de escribir una sola línea de código, debes entender qué hace EXCELENTE a un portafolio de tu nivel.

### 1.2 ¿Qué buscar?

Estudia al menos **10 portafolios** de perfiles similares al tuyo:

```
Términos de búsqueda recomendados:
- "senior software architect portfolio 2024 2025"
- "AI engineer personal website case study"
- "CTO portfolio dark theme"
- "enterprise systems architect website"
- "full stack AI architect portfolio"
```

**Plataformas donde buscar:**

| Plataforma | URL | Qué encontrarás |
|------------|-----|-----------------|
| Dribbble | dribbble.com | Diseños UI visuales |
| Behance | behance.net | Casos de estudio completos |
| Figma Community | figma.com/community | Templates editables |
| GitHub Topics | github.com/topics/portfolio | Código real |
| Awwwards | awwwards.com | Los más premiados |

### 1.3 Plantilla de Análisis (usar por cada portafolio)

Para cada portafolio que analices, responde:

```markdown
## Portafolio analizado: [Nombre/URL]

- [ ] ¿Cómo presenta su identidad en el Hero?
- [ ] ¿Tiene casos de estudio o solo screenshots?
- [ ] ¿Usa métricas de impacto?
- [ ] ¿Qué secciones tiene y en qué orden?
- [ ] ¿Qué animaciones usa?
- [ ] ¿Modo oscuro o claro?
- [ ] ¿Qué me gusta que puedo adaptar?
- [ ] ¿Qué evitaría?
```

### 1.4 Resultado esperado de esta fase
Un **Moodboard** con capturas de pantalla de los elementos que más te inspiraron.

---

## ✍️ FASE 2 — Definición de Contenido y Narrativa

### 2.1 Tu identidad como marca (copy profesional)

Antes de diseñar, define el texto exacto de cada sección.

**Hero — Tu frase principal:**
```
Senior Systems & AI Architect
Diseño arquitecturas inteligentes que integran
software empresarial, IA aplicada y automatización avanzada.
+8 años | 5+ industrias | NASA Hackathon Winner
```

**Subheadline (lo que haces):**
```
Especialista en ERP/CRM Empresarial · Agentes Inteligentes ·
Ciencia de Datos · IoT & Mecatrónica · Ciberseguridad
```

### 2.2 Tus 5 Especializaciones (secciones estratégicas)

Define el contenido exacto de cada módulo:

```markdown
## MÓDULO 1 — Enterprise Software Architect
Proyectos: ERP Construcción, CRM Salud, POS Retail, RRHH, Logística
Industrias: Construcción, Medicina, Ventas, Mecánica, Electricidad

## MÓDULO 2 — AI & Data Science
Proyectos: Publicaciones científicas, Modelos ML, Minería de datos
Herramientas: Python, TensorFlow, Scikit-learn, R

## MÓDULO 3 — Intelligent Agents & Automation
Proyectos: Agentes autónomos, LLMs, Integración APIs
Stack: LangChain, OpenAI, Anthropic, n8n

## MÓDULO 4 — IoT & Mechatronics
Proyectos: Aplicaciones embebidas, Hardware-software
Stack: Arduino, Raspberry Pi, MQTT, C++

## MÓDULO 5 — Cybersecurity & Infrastructure
Proyectos: Redes Cisco, Arquitecturas seguras
Herramientas: Cisco Packet Tracer, Firewalls, VPN
```

### 2.3 Formato de Caso de Estudio (template)

Para cada proyecto importante:

```markdown
## [Nombre del Proyecto] — [Industria]

**El Problema:**
[Descripción del problema empresarial real]

**La Solución:**
[Arquitectura y tecnología aplicada]

**El Impacto:**
- X% reducción en [métrica]
- Y usuarios/transacciones diarias
- Z tiempo de implementación

**Stack tecnológico:** Next.js · PostgreSQL · Docker · Redis

[Ver Demo] [Ver GitHub] [Ver Caso Completo]
```

### 2.4 Métricas que debes tener listas

```
✅ Años de experiencia: 8+
✅ Proyectos completados: [número real]
✅ Industrias atendidas: 10+
✅ Hackathons ganados: NASA + [otros]
✅ Publicaciones científicas: [número]
✅ Países / clientes: [si aplica]
```

---

## 🎨 FASE 3 — Diseño UX/UI en Figma

### 3.1 Configuración del archivo Figma

```
Archivo: portfolio-[tu-nombre].fig

Páginas recomendadas:
├── 📌 Moodboard
├── 🎨 Design System (Tokens)
├── 📐 Wireframes (Low fidelity)
├── 🖥️ Desktop (High fidelity)
├── 📱 Mobile (High fidelity)
└── 🧩 Components Library
```

### 3.2 Design System (definir ANTES de diseñar)

**Paleta de colores recomendada para tu perfil:**

```
Background principal:  #0A0A0F  (negro profundo)
Background secundario: #111118  (gris muy oscuro)
Card / Surface:        #1A1A27  (gris azulado oscuro)
Accent primario:       #6366F1  (índigo/violeta IA)
Accent secundario:     #06B6D4  (cyan tecnológico)
Texto primario:        #F1F5F9  (blanco suave)
Texto secundario:      #94A3B8  (gris claro)
Success / Green:       #10B981
Warning / Amber:       #F59E0B
```

**Tipografías:**

```
Headings: Inter Bold / Sora Bold
Body:     Inter Regular
Code:     JetBrains Mono
```

**Espaciado base:** 8px (múltiplos: 8, 16, 24, 32, 48, 64, 96)

**Breakpoints:**
```
Mobile:  375px
Tablet:  768px
Desktop: 1280px
Wide:    1440px
```

### 3.3 Arquitectura UX — Flujo de navegación

```
[LANDING]
   │
   ├── Hero (Identidad + CTA)
   ├── Stats (Métricas rápidas)
   ├── Especialidades (5 módulos)
   ├── Proyectos Destacados (6 cards)
   ├── Casos de Estudio (3 profundos)
   ├── Tech Stack (visual)
   ├── Publicaciones / Logros
   ├── Timeline profesional
   └── Contacto (CTA final)

[RUTAS INTERNAS]
   /projects/enterprise
   /projects/ai-data
   /projects/agents
   /projects/iot
   /projects/cybersecurity
   /projects/[slug]  ← Caso de estudio individual
   /about
   /publications
   /contact
```

### 3.4 Wireframes — Estructura de cada sección

#### HERO
```
┌─────────────────────────────────────────┐
│  [LOGO/NOMBRE]              [NAV LINKS] │
├─────────────────────────────────────────┤
│                                         │
│   Badge: "Available for projects"       │
│                                         │
│   H1: [Tu nombre]                       │
│   H2: Senior Systems & AI Architect     │
│                                         │
│   Descripción 2 líneas máximo           │
│                                         │
│   [Ver Proyectos]   [Descargar CV]      │
│                                         │
│   ─────────────────────────────         │
│   8+ años   |  50+ proyectos  |  NASA   │
│                                         │
└─────────────────────────────────────────┘
```

#### SECCIÓN ESPECIALIDADES
```
┌─────────────────────────────────────────┐
│        Mis Especializaciones            │
│                                         │
│  [🏢 Enterprise] [🤖 AI] [⚡ Agents]  │
│  [📡 IoT]        [🔐 Cyber]            │
│                                         │
│  ┌────────────────────────────────┐    │
│  │  [Módulo activo - detalle]     │    │
│  │  Descripción + proyectos       │    │
│  │  Stack tecnológico             │    │
│  └────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

#### PROYECTO CARD
```
┌─────────────────────┐
│  [Imagen/Preview]   │
│  Badge: Industria   │
│  ─────────────────  │
│  Nombre Proyecto    │
│  Descripción corta  │
│  ─────────────────  │
│  React · Node · PG  │
│  ─────────────────  │
│  [Case Study] [→]   │
└─────────────────────┘
```

### 3.5 Orden de diseño en Figma

```
1. Diseña el Design System primero (colores, tipo, componentes base)
2. Diseña Desktop Hero
3. Diseña sección Proyectos
4. Diseña caso de estudio individual
5. Diseña resto de secciones
6. Adapta a Mobile
7. Prototipa navegación básica en Figma
```

---

## ⚙️ FASE 4 — Setup del Proyecto Next.js

### 4.1 Requisitos previos

```bash
# Verificar versiones
node --version   # >= 20.x recomendado
npm --version    # >= 10.x
git --version
```

### 4.2 Crear el proyecto

```bash
# Crear proyecto Next.js con todas las opciones modernas
npx create-next-app@latest portfolio --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"

cd portfolio
```

### 4.3 Instalar dependencias esenciales

```bash
# Animaciones profesionales
npm install framer-motion

# Iconos
npm install lucide-react

# UI Components
npm install @radix-ui/react-dialog @radix-ui/react-tabs @radix-ui/react-tooltip

# Utilidades
npm install clsx tailwind-merge

# Fuentes optimizadas
npm install @next/font

# Markdown para publicaciones/blog
npm install next-mdx-remote gray-matter

# Opcional: 3D/Three.js para efectos visuales impactantes
npm install three @types/three @react-three/fiber @react-three/drei
```

### 4.4 Configurar tailwind.config.ts

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
  theme: {
    extend: {
      colors: {
        background: {
          DEFAULT: "#0A0A0F",
          secondary: "#111118",
        },
        surface: "#1A1A27",
        accent: {
          primary: "#6366F1",
          secondary: "#06B6D4",
        },
        text: {
          primary: "#F1F5F9",
          secondary: "#94A3B8",
        },
      },
      fontFamily: {
        sans: ["Inter", "sans-serif"],
        heading: ["Sora", "sans-serif"],
        mono: ["JetBrains Mono", "monospace"],
      },
      animation: {
        "fade-in": "fadeIn 0.6s ease-in-out",
        "slide-up": "slideUp 0.5s ease-out",
        "glow": "glow 2s ease-in-out infinite alternate",
      },
      keyframes: {
        fadeIn: {
          "0%": { opacity: "0" },
          "100%": { opacity: "1" },
        },
        slideUp: {
          "0%": { transform: "translateY(20px)", opacity: "0" },
          "100%": { transform: "translateY(0)", opacity: "1" },
        },
        glow: {
          "0%": { boxShadow: "0 0 20px #6366F140" },
          "100%": { boxShadow: "0 0 40px #6366F180" },
        },
      },
    },
  },
  plugins: [],
};

export default config;
```

---

## 🗂️ FASE 5 — Arquitectura de Carpetas

### 5.1 Estructura completa del proyecto

```
portfolio/
├── src/
│   ├── app/                          ← App Router (Next.js 15)
│   │   ├── layout.tsx                ← Layout global
│   │   ├── page.tsx                  ← Home / Landing
│   │   ├── about/
│   │   │   └── page.tsx
│   │   ├── projects/
│   │   │   ├── page.tsx              ← Todos los proyectos
│   │   │   ├── enterprise/page.tsx
│   │   │   ├── ai-data/page.tsx
│   │   │   ├── agents/page.tsx
│   │   │   ├── iot/page.tsx
│   │   │   ├── cybersecurity/page.tsx
│   │   │   └── [slug]/page.tsx       ← Caso de estudio dinámico
│   │   ├── publications/
│   │   │   └── page.tsx
│   │   └── contact/
│   │       └── page.tsx
│   │
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Navbar.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── PageWrapper.tsx
│   │   │
│   │   ├── sections/                 ← Secciones del Home
│   │   │   ├── Hero.tsx
│   │   │   ├── Stats.tsx
│   │   │   ├── Specializations.tsx
│   │   │   ├── FeaturedProjects.tsx
│   │   │   ├── CaseStudies.tsx
│   │   │   ├── TechStack.tsx
│   │   │   ├── Publications.tsx
│   │   │   ├── Timeline.tsx
│   │   │   └── Contact.tsx
│   │   │
│   │   ├── ui/                       ← Componentes reutilizables
│   │   │   ├── Button.tsx
│   │   │   ├── Badge.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── ProjectCard.tsx
│   │   │   ├── CaseStudyCard.tsx
│   │   │   ├── TechBadge.tsx
│   │   │   ├── MetricCard.tsx
│   │   │   ├── SectionTitle.tsx
│   │   │   └── GradientText.tsx
│   │   │
│   │   └── effects/                  ← Efectos visuales
│   │       ├── ParticleBackground.tsx
│   │       ├── GlowEffect.tsx
│   │       └── AnimatedCounter.tsx
│   │
│   ├── data/                         ← Todo el contenido estático
│   │   ├── profile.ts                ← Tu info personal
│   │   ├── projects.ts               ← Todos los proyectos
│   │   ├── skills.ts                 ← Stack técnico
│   │   ├── experience.ts             ← Timeline laboral
│   │   ├── publications.ts           ← Artículos científicos
│   │   └── achievements.ts           ← Hackathons, premios
│   │
│   ├── services/
│   │   └── github.ts                 ← Integración GitHub API
│   │
│   ├── lib/
│   │   ├── utils.ts                  ← cn() y utilidades
│   │   └── constants.ts              ← Variables globales
│   │
│   ├── types/
│   │   └── index.ts                  ← TypeScript interfaces
│   │
│   └── styles/
│       └── globals.css               ← Estilos globales
│
├── public/
│   ├── images/
│   │   ├── profile/
│   │   ├── projects/
│   │   └── logos/
│   ├── cv/
│   │   └── cv-tu-nombre.pdf
│   └── favicon.ico
│
├── .env.local                        ← Variables de entorno
├── next.config.ts
├── tailwind.config.ts
└── tsconfig.json
```

### 5.2 Interfaces TypeScript base

```typescript
// src/types/index.ts

export interface Project {
  id: string;
  slug: string;
  title: string;
  description: string;
  industry: string;
  category: 'enterprise' | 'ai-data' | 'agents' | 'iot' | 'cybersecurity';
  featured: boolean;
  stack: string[];
  images: string[];
  githubUrl?: string;
  demoUrl?: string;
  problem: string;
  solution: string;
  impact: ImpactMetric[];
  year: number;
}

export interface ImpactMetric {
  label: string;
  value: string;
  icon?: string;
}

export interface Specialization {
  id: string;
  title: string;
  description: string;
  icon: string;
  accentColor: string;
  skills: string[];
  projectCount: number;
}

export interface Publication {
  title: string;
  journal: string;
  year: number;
  url?: string;
  abstract: string;
  tags: string[];
}

export interface Experience {
  company: string;
  role: string;
  period: string;
  description: string;
  achievements: string[];
  stack: string[];
}
```

---

## 🧱 FASE 6 — Implementación por Secciones

### Orden recomendado de implementación

```
Semana 1:
  Día 1: Layout global + Navbar + Footer
  Día 2: Hero Section (la más importante)
  Día 3: Stats + Especialidades
  Día 4: ProjectCard component + FeaturedProjects
  Día 5: Caso de Estudio individual (/projects/[slug])

Semana 2:
  Día 1: TechStack visual
  Día 2: Timeline profesional
  Día 3: Publications + Achievements
  Día 4: Contact section
  Día 5: Pulido, animaciones, responsive
```

### 6.1 Ejemplo: Hero Section

```tsx
// src/components/sections/Hero.tsx
"use client";

import { motion } from "framer-motion";
import { ArrowDown, Download, Github, Linkedin } from "lucide-react";
import { profile } from "@/data/profile";

export default function Hero() {
  return (
    <section className="relative min-h-screen flex items-center justify-center bg-background overflow-hidden">
      
      {/* Fondo con efecto de gradiente */}
      <div className="absolute inset-0 bg-gradient-radial from-accent-primary/10 via-transparent to-transparent" />
      
      <div className="container mx-auto px-6 text-center relative z-10">
        
        {/* Badge de disponibilidad */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.2 }}
          className="inline-flex items-center gap-2 bg-surface border border-accent-primary/30 rounded-full px-4 py-2 mb-8"
        >
          <span className="w-2 h-2 rounded-full bg-green-400 animate-pulse" />
          <span className="text-sm text-text-secondary">Disponible para proyectos</span>
        </motion.div>

        {/* Nombre */}
        <motion.h1
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.3 }}
          className="text-5xl md:text-7xl font-heading font-bold text-text-primary mb-4"
        >
          {profile.name}
        </motion.h1>

        {/* Título profesional */}
        <motion.h2
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.4 }}
          className="text-xl md:text-2xl text-accent-primary font-medium mb-6"
        >
          {profile.title}
        </motion.h2>

        {/* Descripción */}
        <motion.p
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.5 }}
          className="max-w-2xl mx-auto text-text-secondary text-lg mb-10"
        >
          {profile.tagline}
        </motion.p>

        {/* CTAs */}
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: 0.6 }}
          className="flex flex-col sm:flex-row gap-4 justify-center mb-16"
        >
          <a
            href="#projects"
            className="bg-accent-primary hover:bg-accent-primary/90 text-white px-8 py-3 rounded-lg font-medium transition-all hover:shadow-glow"
          >
            Ver Proyectos
          </a>
          <a
            href="/cv/cv-tu-nombre.pdf"
            download
            className="border border-accent-primary/50 text-text-primary px-8 py-3 rounded-lg font-medium hover:border-accent-primary hover:bg-accent-primary/10 transition-all flex items-center gap-2 justify-center"
          >
            <Download size={16} />
            Descargar CV
          </a>
        </motion.div>

        {/* Métricas */}
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.8 }}
          className="flex flex-wrap justify-center gap-8 text-center border-t border-surface pt-8"
        >
          {profile.stats.map((stat) => (
            <div key={stat.label}>
              <div className="text-3xl font-bold text-text-primary">{stat.value}</div>
              <div className="text-sm text-text-secondary">{stat.label}</div>
            </div>
          ))}
        </motion.div>

      </div>
    </section>
  );
}
```

### 6.2 Ejemplo: Data de perfil

```typescript
// src/data/profile.ts

export const profile = {
  name: "Tu Nombre",
  title: "Senior Systems & AI Architect",
  tagline: "Diseño arquitecturas inteligentes que integran software empresarial, IA aplicada y automatización avanzada para empresas en múltiples industrias.",
  location: "Perú",
  email: "tu@email.com",
  github: "https://github.com/tu-usuario",
  linkedin: "https://linkedin.com/in/tu-perfil",
  stats: [
    { value: "8+",  label: "Años de experiencia" },
    { value: "50+", label: "Proyectos completados" },
    { value: "10+", label: "Industrias atendidas"  },
    { value: "3x",  label: "Hackathons ganados"    },
  ],
};
```

### 6.3 Animaciones con Framer Motion — patrón recomendado

```tsx
// Patrón reutilizable para animar secciones al hacer scroll
import { motion } from "framer-motion";
import { useInView } from "framer-motion";
import { useRef } from "react";

const fadeInUp = {
  hidden: { opacity: 0, y: 40 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.6, ease: "easeOut" } },
};

const staggerContainer = {
  hidden: {},
  visible: { transition: { staggerChildren: 0.15 } },
};

// Uso en cualquier sección:
<motion.div
  variants={staggerContainer}
  initial="hidden"
  whileInView="visible"
  viewport={{ once: true, margin: "-100px" }}
>
  {items.map((item) => (
    <motion.div key={item.id} variants={fadeInUp}>
      {/* contenido */}
    </motion.div>
  ))}
</motion.div>
```

---

## 🔗 FASE 7 — Integración GitHub API

### 7.1 Variables de entorno

```bash
# .env.local
GITHUB_USERNAME=tu-usuario-github
GITHUB_TOKEN=ghp_xxxxxxxxxxxx   # Personal Access Token (opcional, aumenta rate limit)
```

### 7.2 Servicio GitHub

```typescript
// src/services/github.ts

const GITHUB_USER = process.env.GITHUB_USERNAME;

export async function getGithubRepos() {
  const res = await fetch(
    `https://api.github.com/users/${GITHUB_USER}/repos?per_page=100&sort=updated`,
    {
      headers: process.env.GITHUB_TOKEN
        ? { Authorization: `token ${process.env.GITHUB_TOKEN}` }
        : {},
      next: { revalidate: 3600 }, // Cache por 1 hora
    }
  );

  if (!res.ok) throw new Error("Error fetching GitHub repos");

  const repos = await res.json();

  // Filtrar y ordenar
  return repos
    .filter((r: any) => !r.fork && r.description)
    .sort((a: any, b: any) => b.stargazers_count - a.stargazers_count);
}

export async function getGithubProfile() {
  const res = await fetch(
    `https://api.github.com/users/${GITHUB_USER}`,
    { next: { revalidate: 86400 } } // Cache por 24 horas
  );
  if (!res.ok) throw new Error("Error fetching GitHub profile");
  return res.json();
}
```

---

## ✅ FASE 8 — Revisión y Pruebas Locales

### 8.1 Checklist antes de considerar terminado

**Performance:**
- [ ] Imágenes optimizadas con `next/image`
- [ ] Fuentes cargadas con `next/font`
- [ ] Animaciones no bloquean el render inicial
- [ ] Sin console.log en producción

**UX/UI:**
- [ ] Responsive: Mobile 375px / Tablet 768px / Desktop 1280px
- [ ] Modo oscuro consistente en todas las secciones
- [ ] Hover states en todos los elementos interactivos
- [ ] Focus states accesibles (outline visible)
- [ ] Texto legible: contraste AAA

**Contenido:**
- [ ] Todos los proyectos tienen descripción real
- [ ] Todos los links funcionan
- [ ] CV descargable actualizado
- [ ] Foto profesional cargada
- [ ] Información de contacto correcta

**Técnico:**
- [ ] Sin errores TypeScript (`npx tsc --noEmit`)
- [ ] Sin errores de ESLint (`npm run lint`)
- [ ] `npm run build` exitoso sin warnings

### 8.2 Comandos útiles en desarrollo

```bash
# Desarrollo
npm run dev             # Servidor local en http://localhost:3000

# Verificaciones
npm run lint            # Revisar errores de código
npx tsc --noEmit        # Verificar TypeScript
npm run build           # Build de producción local

# Limpieza
rm -rf .next && npm run dev  # Si hay problemas de cache
```

---

## 🧭 RESUMEN EJECUTIVO — Checklist General

```
FASE 1 — Investigación
  ✅ Analizar 10+ portafolios de referencia
  ✅ Moodboard con elementos inspiradores
  ✅ Definir patrones a adoptar y evitar

FASE 2 — Contenido
  ✅ Copy del Hero redactado
  ✅ 5 especializaciones definidas con contenido
  ✅ 6 casos de estudio con métricas reales
  ✅ Stats personales confirmados

FASE 3 — Figma
  ✅ Design System completo (colores, tipo, espaciado)
  ✅ Wireframes de todas las secciones
  ✅ Diseño Desktop high fidelity
  ✅ Diseño Mobile high fidelity
  ✅ Prototipo navegación

FASE 4 — Setup
  ✅ Next.js 15 creado con TypeScript + Tailwind
  ✅ Dependencias instaladas (Framer Motion, Lucide, etc.)
  ✅ Tailwind configurado con tu paleta
  ✅ .env.local con variables GitHub

FASE 5 — Arquitectura
  ✅ Estructura de carpetas implementada
  ✅ Types TypeScript definidos
  ✅ Data files creados con contenido real

FASE 6 — Implementación
  ✅ Layout global + Navbar + Footer
  ✅ Hero Section
  ✅ Especialidades / Skills
  ✅ Proyectos (cards + página individual)
  ✅ Timeline + Publications
  ✅ Contact

FASE 7 — GitHub API
  ✅ Servicio de integración
  ✅ Repos filtrados y mostrados

FASE 8 — Revisión
  ✅ Responsive verificado
  ✅ Performance revisada
  ✅ Build exitoso
```

---

> **💡 Consejo Final:** No intentes terminar todo perfecto de una vez.  
> Prioriza que el **Hero + 3 proyectos fuertes** estén impecables.  
> Un portafolio con 3 casos de estudio poderosos supera a uno con 20 proyectos mediocres.  
> **Muestra impacto, no cantidad.**
